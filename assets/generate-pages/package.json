{
  "name": "customgen",
  "version": "1.0.0",
  "description": "Custom static site generator.",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf text/ && rm -rf _site && rm -rf node_modules && cp -r ../text . && cp ../../LICENSE.md text/ && npm install", 
    "build": "npx eleventy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^1.0.2",
    "markdown-it-anchor": "^8.6.5"
  }

}
